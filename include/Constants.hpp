#ifndef CONSTANTS // include guard
#define CONSTANTS

#include <string>

using namespace std;

namespace C
{
    const char SIGNS[4]{'+', '-', '*', '/'};
    const char ADD = '+';
    const char SUB = '-';
    const char MULT = '*';
    const char DIV = '/';
    const char LB = '(';
    const char RB = ')';
} // namespace C

#endif