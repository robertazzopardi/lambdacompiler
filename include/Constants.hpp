#ifndef CONSTANTS // include guard
#define CONSTANTS

#include <vector>

namespace C
{
    static const std::vector<char> SIGNS{'+', '-', '*', '/'};
    const char ADD = '+';
    const char SUB = '-';
    const char MULT = '*';
    const char DIV = '/';
    const char LB = '(';
    const char RB = ')';

} // namespace C

#endif