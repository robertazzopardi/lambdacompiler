#ifndef CONSTANTS // include guard
#define CONSTANTS

#include <string>

namespace C
{
    const char ADD = '+';
    const char SUB = '-';
    const char MULT = '*';
    const char DIV = '/';
    const char BL = '(';
    const char BR = ')';
} // namespace C

#endif